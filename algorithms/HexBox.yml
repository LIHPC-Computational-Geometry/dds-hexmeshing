tet-mesh: { # case of 'HexBox' applied on a 'tet-mesh' subfolder
  executable: {
    path: HexBox,
    filename: ,
    command_line: '{mesh}'
  },
  tee: true,
  output_folder: 'HexBox_%d',
  arguments: {
    input_files: {
      mesh: SURFACE_MESH_OBJ
    }
  }
}